# W000-P01 Evidence — Godot Project and Test Harness

Status: `VERIFIED`

Coordinator verification commit: `cdd58b4bb1939af172024b36244d327fa8a4a775`

Independent reviews: `SPEC PASS`, `QUALITY PASS`. The coordinator reran the complete toolchain gate after both reviews and observed the same exit codes with no residual Godot process.

## Locked inputs

- Godot: `4.7.2.stable.official.ed1daf0bf`
- Godot Win64 archive SHA-256: `731980F9608D61333E5BAF54A2EF17210ACC7A538446C0CB9969F002ACA1E953`
- GUT: `9.7.1`
- GUT source archive SHA-256: `14969AA46ADC84AA08CDD21B9F6D1A64ADDD92AE60B36F02D0521ED305AA4086`
- Vendored GUT file count: `259`; upstream and vendored `LICENSE.md` SHA-256 both `872FEA6E72319F72E4EC0AA10BD7EC535ED3124BF2718BD8E0140E9ADAFEF917`.

## RED

The first harness attempt exposed a prerequisite rather than the product failure: GUT reported missing imported `class_name` entries and returned `0`. `Run-Gut.ps1` was therefore corrected to run a headless Godot import before invoking GUT. This attempt is not counted as RED.

With the App scene intentionally still absent, the real RED command was:

```powershell
$env:STARSOIL_GODOT_EXE = 'C:\Users\loasu\Desktop\test\GAME\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe'
pwsh -NoProfile -File .\scripts\Run-Gut.ps1
```

Observed result on 2026-08-28:

```text
res://tests/unit/test_app_bootstrap.gd
0/2 passed.
Cannot open file 'res://scenes/app.tscn'.
RED_EXIT_CODE=1
```

This was the expected failure: the tests could load and run, but the required App scene and host nodes had not been implemented.

## GREEN

After adding `scenes/app.tscn` and strongly typed `src/app/app.gd`, the same default-suite command produced:

```text
2/2 passed.
GREEN_EXIT_CODE=0
```

A runtime project-settings assertion was then added. Final verification command:

```powershell
$env:STARSOIL_GODOT_EXE = 'C:\Users\loasu\Desktop\test\GAME\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe'
pwsh -NoProfile -File .\scripts\Verify-Toolchain.ps1
```

Observed final results:

```text
VERSION PASS 4.7.2.stable.official.ed1daf0bf
IMPORT PASS exit=0
MAIN_SCENE PASS exit=0
3/3 passed; 15 assertions
GUT_DEFAULT PASS exit=0
0/1 passed (isolated intentional fixture)
GUT_FAILURE_FIXTURE PASS exit=1
```

The deliberate failure is excluded from the default suite and proves that the CI-facing runner propagates a non-zero test failure exit code.
