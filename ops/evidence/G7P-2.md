# G7P-2 — S 级遗留清理（复审登记边缘耦合点全部数据化）

- 分支：`feature/g7p2-cleanup`（worktree `.worktrees/g7p2`，基线 `main@b3f890f`，631/631 全绿起点）
- 日期：2026-09-01
- 任务书：复审章节遗留清单 S1-S10 + M11/M12（docs/plans/2026-08-31-dlc-readiness-and-pm-verdict.md 复审裁决）
- 目标：把"新内容种类接入需 1 处小代码"的项再消掉一批，全部数据化，行为逐项等价，不破坏基线。

## 1. 处置总表

| 项 | 内容 | 处置 | 提交 | 关键交付 |
|---|---|---|---|---|
| S1 | ending_ready 硬编码三胜利旗标 | **已消** | f551d34 | `data/progression/ending_gate.json` + `schemas/ending-gate.schema.json`；Progression 读表（event_chain 同 bootstrap 模式）；防漂移同步测试 |
| S3 | 信任门提示单文件读取 | **已消（加分项）** | 38ba27b | Hud 扫描 `data/events/*.json` 全目录自动派生门提示 |
| S4 | 遭遇道具上限/可装配判定硬编码 | **已消** | 7289ed5 | 遭遇可选 `max_items_per_type`（缺省 2）；`FROZEN_SANDBOX_BATTLE_ITEMS` 删除，battle_usable 全数据判定 |
| S5 | 角色白名单/面板行硬编码 | **已消** | 958b8ef | `data/content/characters.json` + `schemas/character.schema.json`；Relations/Hud 同源读表 |
| S9 | story_key 死内容哨兵无消费判定 | **已消** | 3120085 | `validate_refs` 增加消费扫描，dangling `unreferenced_story_key` |
| S10 | objectives/hints 不进 content_hash | **已消** | 95e07c5 | HASH_CONFIG_FILES +2；golden 重生成；契约注释同步 |
| M11 | 相机限界静态（场景文件） | **已消** | 6d4c021 | World._ready 按配置网格运行时覆盖 Camera2D limit_*；world.tscn 未改 |
| M12 | built 反应 payload def/自装载表双路径 | **已消** | 7ebc2c1 | 回退路径删除，payload def 唯一权威（缺失 → 失败安全 no_op） |
| S2/S6/S7/S8 | （复审登记其余 S 项） | **本波未派发** | — | 任务书仅含上列 8 项；其余维持登记（见 §7 限制与下一步） |

## 2. 逐项 RED / GREEN（命令 + 新鲜输出）

统一命令前缀：`cd /c/Users/loasu/Desktop/test/GAMEGLM/.worktrees/g7p2`；Godot 直调
`"C:\Users\loasu\Desktop\test\GAMEGLM\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe" --headless --path . -s res://addons/gut/gut_cmdln.gd -gexit -gtest=<file>`。

### S1
- RED：新增 `tests/unit/test_progression_ending_gate.gd`（7 用例）→ 输出 `Tests 7 / Passing 1 / Failing 6`，逐条 `Invalid call. Nonexistent function 'load_ending_gate_from' in base 'GDScript'`。
- GREEN：同一命令 → `Passing 7 / Asserts 66`；`python scripts/validate_content.py` → `Checked 37 content file(s), 62 definition(s) against 12 schema(s). RESULT: PASS`。
- 等价证明：门表旗标集断言逐字节等于旧三常量（`encounter_first_drift_won`/`encounter_husk_ambush_won`/`encounter_leviathan_won`）；`test_progression.gd` 既有 ending_ready 快照零修改全绿；station_mode_* 前缀守卫保留在代码侧（链前缀词汇表规范来源）；空表=永不满足（非空真）由坏表用例锁定。
- 附加：S1 即把 `ending_gate.json` 纳入 HASH_CONFIG_FILES（与 event_chain 同类的进度配置），golden 随之重生成（见 §5）。

### S4
- RED：新增 `tests/unit/test_encounters_item_policy.gd`（8 用例）→ `Tests 8 / Passing 4 / Failing 4`（上限覆盖、无 item_defs 语义、ContentDB 字段接受为红灯）。
- GREEN：`Passing 8`；`test_encounters_director/economy/battle_scene/battle_presentation/content_data/content_db` 合跑全绿；validate_content.py PASS。
- 等价证明：三场生产遭遇均未声明 `max_items_per_type` → 缺省 2，`EXPECTED_ALLY_ITEM_COUNTS`（test_content_data.gd 冻结快照）零修改通过；生产路径 `GameSession._battle_content` 始终从 ContentDB 传入全量 item_defs → 装配结果与迁移前一致。唯一断言更新：`test_start_without_item_defs_falls_back_to_frozen_sandbox_items` → `test_start_without_item_defs_equips_nothing`（冻结兜底清单随数据化删除，无数据源 = 失败安全不装配）。
- 最小侵入裁决（任务书要求 evidence 说明）：未给 EncounterDirector 增加 ContentDB 依赖（保持静态纯函数、不触 autoload），沿用既有 `content["item_defs"]` 参数化缝；生产侧数据源本就存在，无需新接线。

### S5
- RED：新增 `tests/unit/test_characters_table.gd`（6 用例）→ `Tests 6 / Passing none / Failing 6`。
- GREEN：`Passing 6`；`test_relations/test_ui_relations/test_ui_hud` 合跑 62/62 全绿；validate_content.py PASS（13 schema）。
- 等价证明：生产表 = `{luoxian:洛弦, misa:弥砂}` 与旧 `VALID_CHARACTERS`/`RELATION_ROW_IDS`/`RELATION_CHAR_NAMES` 逐值一致；坏表回退缺省 `{luoxian, mistic}`——准确说 `{luoxian, misa}`，白名单永不空失效；`test_relations.gd` 快照零修改通过（`invalid_character` 失败码不变，message 措辞更新为指向数据文件，无测试断言旧措辞）。
- 纯数据扩展：第三角色 `nadia`（accent_color `#88AAFF`）仅凭临时 JSON 即进入关系白名单与 HUD 第三行（零代码）。

### S9
- RED：新增 `tests/unit/test_content_story_key.gd`（6 用例）→ `Tests 6 / Passing 4 / Failing 2`（未消费/仅 recipe 消费两条拒绝路径红灯）。
- GREEN：`Passing 6`；`test_content_db/test_content_data` 合跑全绿。
- 等价证明：非 story_key 物品不参与扫描（`test_plain_item_without_story_key_needs_no_consumer` 锁定既有语义零变化）；生产 `echo_seed` 由 `echo_chamber.inputs` 引用 → 生产内容包通过性断言；失败仍走既有 `dangling_ref` 码，仅追加 `unreferenced_story_key` 前缀消息。扫描范围裁决：仅"建筑 inputs / 事件 grant_items"两处声明式消费点（recipe 输入不算，锁定用例 `test_story_item_consumed_only_by_recipe_still_reports`）。

### S10
- RED：`test_content_db.gd` 的 `test_content_hash_includes_progression_config_files` 追加 objectives/hints 两段突变断言 → `Failing Tests 1`，两条 `objectives.json/hints.json 变化必须改变总哈希` 失败（哈希不变 0ed0fc3f…）。
- GREEN：HASH_CONFIG_FILES +2 → 同文件全绿；golden 重生成（过程见 §5）。
- 等价证明：哈希仍为确定性 canonical 总哈希（缺失/坏文件记 "" 贡献，语义连续）；`test_save_v2` golden 五连测与 `test_save_policy_dlx6` 全绿。
- 范围说明：任务书列"两个文件"（objectives/hints）；S1/S5 新建的 ending_gate/characters 同属进度配置/内容登记，已在其各自提交一并纳入（§5 汇总七文件集），否则哈希指纹出现盲区。

### M11
- RED：新增 `tests/unit/test_world_camera_limits.gd`（3 用例）→ `Tests 3 / Passing 1 / Failing 2`（2x1/6x3 配置下相机仍保持静态 4096/2048）。
- GREEN：`Passing 3`；`test_world_dlx5/gap3/scene/contract` 合跑全绿。
- 等价证明：生产 4x2 配置的运行时覆盖值与 world.tscn 静态值逐字节一致（4096/2048/0/0）；`scenes/world.tscn` 零改动（任务书禁改约束满足）；相机 reparent 发生在限界设置之后，属性随节点保持。

### M12
- 性质：行为等价重构（删除路径无新行为可观察，无独立 RED）。
- 变更：删除 `BUILDING_REACTIONS_PATH`/`load_building_reactions_from`/反应表缓存与校验；`_built_def_of` 只读 `payload.building_def`（缺失/非字典 → 空 def → 成功 no_op 零写入）。
- 等价证明：`test_built_reactions_match_frozen_matrix_with_payload_def` 冻结矩阵（9 行，DLX-3 迁移快照）逐字节一致；生产路径 `GameSession.request_place` 本就携带 def（`src/integration/game_session.gd:355`），集成行为零变化；`test_integration.gd` 建造链全绿。
- 测试合法更新（逐条）：`test_progression.gd` 四个 built 用例改传生产 buildings.json 定义（断言不变）；`test_progression_reactions.gd` 旧"无 def 回退表"矩阵改为"无 def 失败安全 no_op"矩阵（9 行零写入断言）；随回退路径删除 3 个用例（反应表装载/缺失文件/坏文件矩阵），新增 1 个用例（非字典 def 失败安全）。净变化 -2 用例。

### S3（可选加分项，已实施）
- 实现：`_gates_from_policy_event`（单文件）→ `_gates_from_events_dir`（目录扫描 *.json，排序确定性），共用 `_read_gate_json_file`；生产门集不变（仅 event_policy.json 含 requires_trust 对象选项 → 仍 2 条门，`test_gate_table_loads_thresholds_from_production_data_files` 零修改通过）。
- TDD 偏差（诚实记录）：本项与锁定测试同批落地，未单独观察 RED；等价性由生产门集不变测试 + 新增多文件派生用例 `test_gate_hints_derive_from_all_event_files_in_dir` 锁定。

## 3. 全量门禁（新鲜输出）

| 门禁 | 结果 |
|---|---|
| `pwsh -NoProfile -File ./scripts/Run-Gut.ps1` | 首轮 660 tests / 659 pass / 1 fail（瞬态，未捕获名称，见 §7）；随后连续 3 轮 **660/660 全绿、10234 asserts**（33.0s / 31.7s / 34.1s） |
| `python scripts/validate_content.py` | `Checked 38 content file(s), 63 definition(s) against 13 schema(s). RESULT: PASS`（exit 0） |
| `pwsh -NoProfile -File ./scripts/Verify-Toolchain.ps1` | exit 0 |
| `pwsh -NoProfile -File ./scripts/Verify-Slice.ps1` | exit 0，含 `GATE4 EXPORT_SMOKE PASS`（build/starsoil/starsoil.exe 导出冒烟） |

测试数核算：基线 631 − 2（M12 删除回退路径用例净减）+ 30（五个新测试文件：7+8+6+6+3）+ 1（test_ui_relations 多文件派生用例）= **660**。

## 4. content_hash 语义终态

`ContentDB.HASH_CONFIG_FILES`（七进度/登记配置文件，canonical JSON 总哈希贡献）：
`content/endings.json`、`content/characters.json`、`progression/event_chain.json`、`progression/ending_gate.json`、`progression/objectives.json`、`progression/hints.json`、`world/world_config.json`。
各文件仍由对应模块自身装载与校验（装载路径与失败回退不变），ContentDB 只做哈希贡献；缺失/空/坏 JSON 记 "" 贡献，哈希确定性保持。

## 5. golden fixture 重生成过程（三次，随哈希集扩展）

1. 每次哈希集变化后重跑生成器：
   `Godot --headless --path . --script res://tests/golden/generate_golden_v1.gd`（真实 `ContentDB.bootstrap("res://data").content_hash()`）。
2. 取 stdout 末行单行 JSON，剥离尾部换行（原 fixture 无尾换行、1061 字节），原样写入 `tests/golden/save_v1_golden.json`。
3. 每次以 python 复核 diff：payload 仅 `content_hash` 变化、envelope `checksum` 随动、其余字段逐字节不变。
   - S1 后：`44fc1742…` → `838973af…`
   - S5 后（characters 入哈希）：`838973af…` → 中间值（过程同上，diff 复核仅 content_hash/checksum）
   - S10 后（objectives/hints 入哈希）：→ 终值 `512d461d2696ee884ee522e41ea81c5368dbdd62ea4958acfe03583a9ee22c1e`
4. 每次重生成后 `test_save_v2.gd`（golden 五连测）与 `test_save_policy_dlx6.gd`（fixture=bootstrap 总哈希契约）即时复跑全绿。
5. 过程教训（首次踩坑）：Windows 管道捕获引入尾随 `\r`，导致 `test_golden_fixture_reencodes_to_identical_envelope` 一度过红；定位后统一 `rstrip(b"\r\n")` 复写，复跑全绿。

## 6. 变更清单（按提交）

- **f551d34 S1**：data/progression/ending_gate.json、schemas/ending-gate.schema.json、scripts/validate_content.py、src/progression/progression.gd、src/content/content_db.gd（HASH_CONFIG_FILES）、tests/unit/test_progression_ending_gate.gd、tests/golden/save_v1_golden.json
- **7289ed5 S4**：src/encounters/encounter_director.gd、src/content/content_db.gd、schemas/encounter.schema.json、tests/unit/test_encounters_item_policy.gd（新）、tests/unit/test_encounters_director.gd（1 条合法更新）
- **958b8ef S5**：data/content/characters.json、schemas/character.schema.json、scripts/validate_content.py、src/content/content_db.gd（RESERVED_DATA_FILENAMES + HASH_CONFIG_FILES）、src/relations/relations.gd、src/ui/hud.gd、tests/unit/test_characters_table.gd（新）、tests/golden/save_v1_golden.json
- **3120085 S9**：src/content/content_db.gd（validate_refs 扫描）、tests/unit/test_content_story_key.gd（新）
- **95e07c5 S10**：src/content/content_db.gd（HASH_CONFIG_FILES +2）、tests/unit/test_content_db.gd（+2 突变断言）、tests/unit/test_save_policy_dlx6.gd（注释同步）、tests/golden/generate_golden_v1.gd（注释同步）、tests/golden/save_v1_golden.json
- **6d4c021 M11**：src/world/world.gd（_apply_camera_limits）、tests/unit/test_world_camera_limits.gd（新）
- **7ebc2c1 M12**：src/progression/progression.gd（回退路径删除）、tests/unit/test_progression.gd（4 用例改传 def）、tests/unit/test_progression_reactions.gd（矩阵语义更新，净 -2 用例）
- **38ba27b S3**：src/ui/hud.gd（目录扫描门派生）、tests/unit/test_ui_relations.gd（注入形态更新 +1 用例）

## 7. 限制与偏差（逐条）

1. **首轮全量 Run-Gut 一次瞬态失败**：660 中 1 条（名称未捕获，输出仅留 tail）；随后连续 3 轮全量 660/660 全绿未复现。怀疑为时序敏感表现层用例在连续重载下的偶发（无本次改动文件的证据——受影响文件级测试均单独复跑全绿）。处置：按"新鲜输出"纪律以最近 3 轮全绿为准；若再现需以 `-gtest` 隔离捕获。
2. **docs/save-content-policy.md 未随哈希集更新**（docs/** 不在本波允许路径）：政策文本仍写"三配置文件"。权威哈希集以 `ContentDB.HASH_CONFIG_FILES` 注释与生成器/契约测试注释为准；文档同步登记为下一步（需 coordinator 授权路径扩展）。
3. **S3 未单独观察 RED**（可选加分项，实现与锁定测试同批落地）：等价性由生产门集不变测试 + 新派生用例锁定。
4. **M12 无独立 RED**（行为等价重构，删除路径无可观察新行为）：等价性由冻结矩阵 + 全量门禁证明；删除的 3 个回退路径用例与语义更新逐条列于 §2 M12。
5. **characters.json accent_color 无生产值/无消费者**：字段已过 schema/运行时校验并有测试覆盖（第三角色用例携带）；生产两名同伴未填色（不发明无消费者的内容值）。
6. **S2/S6-S8 未派发**：任务书范围外，遗留清单维持登记。
7. **scenes/world.tscn、src/state/**、src/save/**、project.godot 零改动**（任务书禁改约束逐项满足；golden fixture 与 tests/golden/generate_golden_v1.gd 的改动系 S10"golden 重生成"任务的直接交付要求）。

## 8. 确切下一步

1. 复审 S2/S6-S8 的具体登记内容（本波任务书未附原文），按同法派发剩余边缘耦合点。
2. coordinator 授权后同步 `docs/save-content-policy.md` 的哈希集措辞（七文件集）。
3. 若全量 Run-Gut 再现瞬态失败：以 `-gtest` 隔离定位并加固（优先怀疑时序敏感表现层用例）。
4. 观察项：ending_gate.json 的旗标 ⊆ encounters.json on_victory_flag 同步测试已在 S1 落地；S9 扫描范围（inputs/grant_items 两消费点）如需扩展到 recipe 输入，属新裁决。
