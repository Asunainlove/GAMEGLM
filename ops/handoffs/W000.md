# W000 Handoff

## Status

Window active. Repository governance has started; toolchain and Walking Skeleton remain to be verified.

## Resume protocol

1. Read `AGENTS.md`, `ops/GOAL.md`, and `ops/state.json`.
2. Confirm the current commit equals `last_verified_commit` once that field is populated.
3. Run the commands recorded in the latest packet evidence.
4. Resume only from `resume_from` and `next_packet_ids`.

## Checkpoints

No verified implementation checkpoint yet.

