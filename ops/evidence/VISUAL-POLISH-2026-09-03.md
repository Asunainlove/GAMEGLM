# Visual Polish (HUD / Theme / Title) — 2026-09-03

Status: `READY` (branch `feat/visual-polish-hud` @ `0c786d1`)

Base: `origin/main` tip `732d34a`

Standing auth: 幕僚长 (2026-09-03) — layout/theme polish; merge if gates OK. No G7 fabrication.

## Goal

Make the vertical slice look less greybox/crude while staying **original Starsoil / 余辉**
aesthetic: denser readable HUD, wired approved batch3 UI panels/buttons + Noto Sans SC
subset. Not copying Terraria / Stardew Valley UI/layout/icons/terms.

## Shipped

| Area | What |
|---|---|
| `themes/starsoil_theme.tres` | Default font → `assets/fonts/NotoSansSC-Regular.subset.otf` (`FontFile`). Panel + Button styles → `StyleBoxTexture` from approved `panel_menu` / `btn_{normal,hover,pressed,disabled}`. Gold hover/pressed font colors per `docs/art/ui-assets.md` tokens. |
| `scenes/ui_hud.tscn` | Inventory/menu/help panel StyleBoxTexture overrides (`panel_inventory` / `panel_menu` / `panel_help`). Margins, separations, objective/relations/build-bar readability. Help footer no longer claims “正式美术尚未接入”. |
| `scenes/title_screen.tscn` | Title-first hierarchy, warmer gold title + shorter gold rule, taller buttons, help panel texture, same help footer polish. |
| `scenes/dialogue_box.tscn` | Theme + `panel_dialog` StyleBoxTexture; padding/separation. |
| `tests/unit/test_ui_theme.gd` | Contract updated: embedded Noto FontFile + StyleBoxTexture (replaces SystemFont / StyleBoxFlat greybox asserts). |
| `THIRD_PARTY_NOTICES.md` | Noto Sans SC OFL entry. |

## Deferred (next PR)

| Item | Why |
|---|---|
| `ops/art-staging/env_v2/*` (soil_base / soil_crack / rock_wall / ui_panel_frame) | Raw GenerateImage outputs are **1536×1024** (~1–1.6MB). Contract needs soil **32×32**, rock wall atlas **384×32**, panels **~96×96 / 256×96**. Naive downscale is unsafe for tile seam / atlas cell layout; leave in staging. |
| `ops/art-staging/v2-polish/` sized drafts | Soil 32×32 and panel_dialog 256×96 exist but need art-approval row + QA before replacing approved batch1/batch3 assets. |
| Mountains of untracked `.import` | Regenerated by Godot `--import`; not committed. |

World TileSet already probes denser env tiles via `WorldRenderer.CELL_ASSET_PROBES`; no TileSet rewrite in this PR.

## Gates (local, 2026-09-03 UTC+8 ~20:00)

| Check | Result |
|---|---|
| Godot `--headless --import` | **PASS** exit 0 |
| HUD/theme subset GUT (`test_ui_theme` + `test_ui_hud`) | **33/33 PASS** exit 0 |
| Default unit suite `-gdir=res://tests/unit` | **715/715 PASS** exit 0 (~23s) |
| `python scripts/validate_content.py` | **PASS** (40 files / 65 defs / 14 schemas) |

Logs: `/workspace/gut-logs/gut-visual-polish-subset.log`, `gut-visual-polish-full.log`, `import-visual-polish.log`

## Notes

- Greybox isolation tests for missing icons / battle sprites remain injection-based and stayed green.
- `project.godot` left on main tip (audio bus + physics ticks preserved); no display-section churn in this commit.
