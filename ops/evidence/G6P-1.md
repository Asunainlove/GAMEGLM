# G6P-1 — 资产适配层（G6 正式美术零代码 drop-in）

- 分支：`feature/g6p1-assets`（worktree `.worktrees/g6p1`，基点 `main@6ce44eb`，660/660 起点）
- 日期：2026-09-01
- 任务书：G6P-1 资产适配层（统一资产解析器 + 世界/战斗/HUD 三挂点 drop-in + 文档落点核对）
- 目标：让 docs/art 四份合同（A6 环境 / A7 UI / A8 战斗 / A5 角色）的正式美术**零代码接入**——资产文件存在即加载使用，缺失即回退现有灰盒（行为逐字节一致）。适配层只负责**加载**：不生产资产、不创建占位资产文件（AGENTS.md：未 approved 资产不得提交到 assets/，`assets/art/**` 本包保持不存在）。

## 1. 处置总表

| 任务 | 内容 | 关键交付 | 测试 |
|---|---|---|---|
| T1 统一资产解析器 | `src/assets/asset_adapter.gd`（class_name AssetAdapter，全静态） | `texture()` / `texture_at()` / `sprite_frames()` / `probe()`；前缀分类 env_/ui_/battle_/char_ → world/ui/battle/characters；合同子目录优先 + 任务书平铺兜底；缺失一律 null | `test_asset_adapter.gd` 19 用例 |
| T2 世界渲染器适配 | `world_renderer.gd` `build_tile_set(base_dir)` 逐源独立探测 5 类格纹理；`last_asset_report` 记账；混合态一次性 `push_warning` 汇总 | `test_world_renderer_assets.gd` 9 用例 |
| T3 战斗单位适配 | `battle_scene.gd` `_build_unit_sprite`：命中 → AnimatedSprite2D（idle 循环，底边中心对齐灰盒底边 +20 px）替换 Box，血量/状态 Label 保留；缺失 → 灰盒逐字节不变；失稳闪紫经 self_modulate | `test_encounters_battle_assets.gd` 3 用例 |
| T4 HUD 物品图标适配 | `hud.gd` `_item_icon`：`ui_item_<item_id>` 命中 → 槽文本左侧 24×24 TextureRect；缺失隐藏（布局不塌）；混合态一次性告警 | `test_ui_hud_item_icons.gd` 5 用例 |
| T5 文档落点核对 | 本文件 §3 drop-in 清单对照表 | — |

测试数核算：基线 660 + 36（19+9+3+5）= **696**。

## 2. 逐任务 RED / GREEN（命令 + 新鲜输出）

统一命令前缀：`cd /c/Users/loasu/Desktop/test/GAMEGLM/.worktrees/g6p1`；单文件跑法
`"C:\Users\loasu\Desktop\test\GAMEGLM\.tools\godot-4.7.2\Godot_v4.7.2-stable_win64_console.exe" --headless --path . -s res://addons/gut/gut_cmdln.gd -gexit -gtest=<file>`。

### T1 AssetAdapter
- RED：新增 `tests/unit/test_asset_adapter.gd`（19 用例）→ 实现文件不存在，GDScript 解析失败：
  `ERROR: Failed to load script "res://tests/unit/test_asset_adapter.gd" with error "Parse error"` +
  `[GUT ERROR]: Nothing was run.`（全新类文件的 RED 形态：契约文件缺失即整文件不可装载；GUT exit 0 属"Nothing was run"语义，如实记录）。
- GREEN：`src/assets/asset_adapter.gd` 落地（同命令）→ `Tests 19 / Passing 19 / Asserts 131`，exit 0。
- 中途修正（TDD 过程内，均属测试侧）：`ADAPTER` 常量类型为 `Script`，返回值需显式 `Texture2D` 标注（强类型纪律）；测试像素断言改为 8-bit 量化容差比较（`Color(0.5,…)` 经 PNG 8-bit 量化为 0.498，纯色值比较不可用精确相等）。

### T2 world_renderer
- RED：新增 `tests/unit/test_world_renderer_assets.gd`（9 用例）→ 强类型契约错误在装载期暴露：
  `Parse Error: Too many arguments for "build_tile_set()" call. Expected at most 0 but received 1.`（×4 处）
  `Parse Error: Static function "partial_asset_warning()" not found in base "WorldRenderer".`（`DEFAULT_ASSET_BASE_DIR` 同样未定义）→ Nothing was run（exit 0 如实记录）。
- GREEN：`world_renderer.gd` 落地 → `Tests 9 / Passing 9 / Asserts 81`，exit 0；既有 `test_world_renderer.gd` 全绿（Asserts 1072）。
- 过程修正：report 累积器改用 Array（PackedStringArray 是值类型，字典内 append 丢失回写）；单色像素断言同 T1 量化容差。

### T3 battle_scene
- RED：新增 `tests/unit/test_encounters_battle_assets.gd`（3 用例）→ 运行期 RED：
  `Tests 3 / Passing 1 / Failing 2 / Asserts 24/27`（灰盒等价用例天然通过=基线锁；注入帧的两用例失败——`asset_base_dir` 尚不存在，精灵节点不出现）。
- GREEN：`battle_scene.gd` 落地 → `Tests 3 / Passing 3 / Asserts 39`，exit 0；既有 `test_encounters_battle_presentation.gd`（Asserts 52）与 `test_encounters_battle_scene.gd`（Asserts 108）全绿。

### T4 hud.gd
- RED：新增 `tests/unit/test_ui_hud_item_icons.gd`（5 用例）→ 装载期契约错误：
  `Parse Error: Static function "partial_icon_warning()" not found in base "Hud".`（×3 处）→ Nothing was run（exit 0 如实记录）。
- GREEN：`hud.gd` 落地 → `Tests 5 / Passing 5 / Asserts 35`，exit 0；既有 HUD 族测试全绿：`test_ui_hud` 29 用例 / `test_ui_objectives` 6 / `test_ui_hints` 10 / `test_ui_relations` 13。

## 3. G6 资产 drop-in 清单（合同落位 → 适配层探测点对照表）

约定（适配层头部注释同源）：**资产落位 = docs/art 合同；未 approved 的资产不得提交到 assets/（AGENTS.md）——适配层只负责加载**。三份合同的可接线面 = G6P-1 四个探测点；其余合同条目为后续接线包（§9.5-R1..R10 / A4 表现包），本表一并列出以便生产方对位。

探测根恒为 `res://assets/art`。探测顺序一律**合同落位优先、任务书平铺兜底**。

### 3.1 环境合同（docs/art/environment-assets.md，A6）→ `world_renderer.CELL_ASSET_PROBES`

| 合同条目 | 资产 ID | 应落 res:// 路径 | 适配层探测点 | G6P-1 状态 |
|---|---|---|---|---|
| ENV-01 | `env_world_soil_base` | `assets/art/world/tiles/env_world_soil_base.png` | source 0 候选 1 | **已接线**（drop-in 即替换 soil 单色） |
| ENV-01 兜底 | — | `assets/art/world/tilesets/soil.png` | source 0 候选 2 | 兜底位 |
| ENV-02 | `env_world_soil_trans_atlas` | `assets/art/world/tiles/env_world_soil_trans_atlas.png` | 无（terrain autotile 接线属 R3 范畴） | 未接线（放置静默，不报错） |
| ENV-03 | `env_world_soil_pit` | `assets/art/world/tiles/env_world_soil_pit.png` | 无（R1 采空坑底接线包） | 未接线 |
| ENV-04 | `env_ore_dust_set`（160×32 五格） | `assets/art/world/tiles/env_ore_dust_set.png` | source 1 候选 1 → tile (0,0)=`_s0` 完好帧 | **已接线**（首格；`_s1/_s2/glint` 态切换属 R2） |
| ENV-05 | `env_ore_shard_set` | `assets/art/world/tiles/env_ore_shard_set.png` | source 2 候选 1 → tile (0,0) | **已接线** |
| ENV-06 | `env_ore_core_set` | `assets/art/world/tiles/env_ore_core_set.png` | source 3 候选 1 → tile (0,0) | **已接线** |
| ENV-07 | `env_mine_wall_atlas`（384×32 十二格） | `assets/art/world/tiles/env_mine_wall_atlas.png` | source 4 候选 1 → tile (0,0)=`_tile0` | **已接线**（首格；饰面/autotile 属 R3） |
| ENV-07 兜底 | — | `assets/art/world/tilesets/rock_wall.png` | source 4 候选 2 | 兜底位（三矿同理有 `world/tilesets/<格型>.png` 兜底） |
| ENV-08..11 | `env_mine_floor_corridor/shaft/chamber/boss` | `assets/art/world/tiles/env_mine_floor_*.png` | 无（R3 新增 source 5..8） | 未接线 |
| ENV-12..14 | `env_overlay_outline_dust/shard/core` | `assets/art/world/tiles/env_overlay_outline_*.png` | 无（R4 OreHighlight 层） | 未接线 |
| ENV-15 | `env_fx_break_seq`（`_f0.._f2`） | `assets/art/world/fx/env_fx_break_seq_f0.png` 等 | 无（R5 cell_break 特效包；帧为单后缀命名，不经 sprite_frames 通用态命名） | 未接线 |
| ENV-16/17 | `env_fx_particle_chip` / `env_fx_particle_mote` | `assets/art/world/fx/…` | 无（R5） | 未接线（texture() env_ 前缀候选已含 world/fx，放置即可被 texture() 解析） |
| ENV-18..20 | `env_bg_sky_afterglow` / `env_bg_crystal_veins_far` / `env_bg_mist_band` | `assets/art/world/background/…` | 无（R9 Parallax 接线包；texture() 候选已含 world/background） | 未接线（可解析） |
| ENV-21..26 | `env_bld_<id>_powered/_unpowered`（或 96×48 合并图集） | `assets/art/world/buildings/…` | 无（R6 建筑精灵接线包；texture() 候选已含 world/buildings） | 未接线（可解析） |
| ENV-27 | `env_fx_build_dust_seq`（`_f0/_f1`） | `assets/art/world/fx/…` | 无（R6） | 未接线（可解析） |
| ENV-28 | `env_border_wall_atlas` | `assets/art/world/tiles/env_border_wall_atlas.png` | 无（R7 WorldBorder 层） | 未接线 |
| ENV-29 | `env_boss_sigil_dormant/_active` | `assets/art/world/decals/env_boss_sigil_*.png` | 无（R8 接线包；texture() 候选已含 world/decals） | 未接线（可解析） |

逐源独立回退语义：任一 source 命中即替换该 source，其余保持冻结单色（互不牵连）；全缺失（生产基态）静默且 TileSet 与灰盒基线逐字节等价；混合态一次性 `push_warning` 汇总缺失格型清单。

### 3.2 战斗合同（docs/art/battle-assets.md，A8）→ `AssetAdapter.sprite_frames` / `battle_scene`

单位帧命名（A8 §7.1）：`<unit_id>_<state>_<NN>.png`，NN 两位零填充、从 00 起；帧节奏 idle 2fps 循环 / attack 8fps / hit 6fps / death 6fps（A8 §7.3，适配层内建）。G6P-1 任务 3 的单位 id → 资产 id 映射：**`battle_<unit_id>`**。

| 合同条目 | 资产 ID（=数据 unit_id） | 应落 res:// 路径（8 帧各一条） | 适配层探测点 | G6P-1 状态 |
|---|---|---|---|---|
| §2.1 洛弦 | `luoxian_fighter` | `assets/art/battle/units/luoxian_fighter/luoxian_fighter_idle_00.png` … death_01.png | `sprite_frames("battle_luoxian_fighter")` 候选 1 | **已接线** |
| §2.2 弥砂 | `misa_weaver` | `assets/art/battle/units/misa_weaver/…` | 同上 | **已接线** |
| §2.3 漂游幼群 | `drift_swarmling` | `assets/art/battle/units/drift_swarmling/…` | 同上 | **已接线** |
| §2.4 碎壳游荡者 | `shard_husk` | `assets/art/battle/units/shard_husk/…` | 同上 | **已接线** |
| §2.5 脉守回响 | `veinwarden_echo` | `assets/art/battle/units/veinwarden_echo/…`（192×192） | 同上 | **已接线** |
| §2.6 辉光利维坦 | `lumen_leviathan` | `assets/art/battle/units/lumen_leviathan/phase1/…`（phase2/ 同名双套） | 候选 2 = `units/lumen_leviathan/phase1/` | **phase1 已接线**；phase2 相位切换属后续表现包 |
| §3 场景底图 | `bg_quarry_surface` / `bg_mine_depths` | `assets/art/battle/backgrounds/bg_*.png` | 无（战斗底图挂点属 A4 表现包；texture_at 全路径可接） | 未接线 |
| §4 行动图标 | `icon_*` ×10 | `assets/art/battle/icons/icon_*.png` | 无（A4 表现包） | 未接线 |
| §5 特效 | `fx_*` ×6 | `assets/art/battle/fx/fx_*_NN.png` | 无（特效播放器属表现包；`AssetAdapter.sprite_frames` 的通用候选含 `battle/fx`，放置即可被解析） | 未接线（可解析） |
| §6 横幅 | `banner_*` ×4 | `assets/art/battle/banners/banner_*.png` | 无（A4 横幅挂点；texture() battle_ 前缀候选含 battle/banners） | 未接线（可解析） |

任务书平铺兜底：`assets/art/battle/battle_<unit_id>_<state>_<NN>.png`（sprite_frames 最终候选，测试注入同形态）。

### 3.3 UI 合同（docs/art/ui-assets.md，A7）→ `Hud._item_icon`

| 合同条目 | 资产 ID | 应落 res:// 路径 | 适配层探测点 | G6P-1 状态 |
|---|---|---|---|---|
| §9 星壤尘图标 | `UIA-ICO-STARSOIL_DUST` | `assets/art/ui/icons/uia_ico_starsoil_dust.png` | `texture("ui_item_starsoil_dust")` 候选 1 → 物品槽 24×24 图标 | **已接线** |
| §9 其余五枚 | `UIA-ICO-LUMEN_SHARD` / `RESONANT_CORE` / `ECHO_SEED` / `SEDATIVE_MIST` / `SHOCK_TRAP` | `assets/art/ui/icons/uia_ico_<item_id>.png` | 同上（每物品独立探测，缺失隐藏不塌） | **已接线** |
| §10.1 尺寸变体 | — | `assets/art/ui/icons/uia_ico_<item_id>_32.png` | 候选 2 | 兜底位 |
| §9 任务书平铺 | — | `assets/art/ui/ui_item_<item_id>.png` | 候选 3 | 兜底位 |
| §3.2 图标底座 | `UIA-HUD-ICON-BASE` | `assets/art/ui/hud/uia_hud_icon_base.png` | 无（底座叠加属后续打磨包；texture() ui_ 前缀候选已含 ui/hud） | 未接线（可解析） |
| §1 面板 / §2 按钮 / §3.1 槽框 / §5 建造栏 / §6 对话框 / §7 标题 / §8 战斗横幅 | `uia_*` | `assets/art/ui/{panels,buttons,buildbar,dialogue,title,battle}/…` | 无（主题迁移 StyleBoxTexture / 挂点升级属 A7 集成包；texture() ui_ 前缀候选已含各子目录） | 未接线（可解析） |
| §4 背包部件 | `uia_inv_*` | `assets/art/ui/inventory/…` | 无（ItemsBox 行首图标超出本包任务范围，属后续打磨包） | 未接线（可解析） |

混合态语义：部分物品命中、部分缺失 → 一次性 `push_warning` 点名缺失物品 id；全缺失（生产基态）静默，槽渲染与基线逐字节一致。

### 3.4 角色合同（docs/art/character-assets.md，A5）— 本包未接，登记边界

A5 的战斗替身帧命名（`_idle_0` 单位后缀、7 帧）与目录（`assets/art/characters/<char>/battle|actions/`、`assets/art/units/<unit>/battle/`）与 A8 战斗合同（`_<state>_<NN>`、8 帧、`battle/units/<unit_id>/`）并存。G6P-1 的战斗单位探测按**必读清单指定的 A8 帧命名约定**（`<asset_id>_<state>_<frame>.png`）与 A8 目录优先；A5 落位资产放置后不会被战斗单位探测点命中（静默、不报错）。A5 的立绘/表情/头像/Cut-in/世界动作集挂点属 W003 后续表现工作包，可用 `AssetAdapter.texture_at()` 全路径或届时扩展适配器（`char_`/characters|units 候选已在 texture() 内预留）。

## 4. 全量门禁（新鲜输出）

| 门禁 | 结果 |
|---|---|
| `pwsh -NoProfile -File ./scripts/Run-Gut.ps1` | **连续 3 轮 696/696 全绿、10520 asserts**（33.9s / 33.3s / 32.5s，exit 0 ×3） |
| `pwsh -NoProfile -File ./scripts/Verify-Toolchain.ps1` | exit 0：VERSION PASS / IMPORT PASS / MAIN_SCENE PASS / GUT_DEFAULT PASS / GUT_FAILURE_FIXTURE PASS exit=1 |
| `python scripts/validate_content.py` | `Checked 38 content file(s), 63 definition(s) against 13 schema(s). RESULT: PASS`（exit 0） |
| `pwsh -NoProfile -File ./scripts/Verify-Slice.ps1` | exit 0：GATE1 GUT PASS / GATE2 TOOLCHAIN PASS / GATE3 EXPORT_PRESETS PASS / **GATE4 EXPORT_SMOKE PASS**（build/starsoil/starsoil.exe 冒烟通过） |

瞬态记录（G6P-1 范围外，如实登记）：本包开发期共 6 轮全量中 2 轮各出 1 个瞬态失败——
- run3：`test_integration.gd test_place_failure_without_materials_shows_craft_hint_once`（合成提示文案为空）；
- run6：`test_integration_dlx4.gd test_echo_chamber_consumes_echo_seed_on_placement`（余辉之种未按预期消耗）。
两例同根因：`GameSession._ready → _try_load_autosave` 载入了**跨运行残留的隔离存档根**——
`user://saves_wp04_integration_<Time.get_ticks_msec()>_<进程内 seq>` 的命名在两次套件运行间可碰撞（msec 对齐 + seq 归零），前次运行残留的 auto.json（含 `hint_craft_seen`/库存）被本次测试的全新 store 读入。属既有测试基建命名缺陷（G7P-2 亦记录过同型瞬态），与本包无关：G6P-1 四文件不触 SaveService、不写任何存档。处置：清理 `user://saves_*` 测试残留根（3941 个目录，环境清理而非仓库改动）后，全量 3 连绿 + 全门禁复绿。

## 5. 等价性证明（资产缺失 = 现状逐字节一致）

- **世界**：`test_build_tile_set_without_assets_matches_greybox_baseline` 锁定——5 source、32×32、tile (0,0)、冻结色值逐源相等（RGB8 量化容差内）、source_id 映射不变；`last_asset_report = {loaded:0, missing:[5 格型]}`；`partial_asset_warning(0,…)==""`（基态静默）。既有 `test_world_renderer.gd` 9 用例（1072 断言）零修改全绿——铺格/擦格/双层语义不变。
- **战斗**：`test_missing_unit_assets_render_greybox` 锁定 Box+Label 结构与文案逐字节；既有 `test_encounters_battle_presentation.gd`（横幅/战报/失稳闪紫/信号时序）与 `test_encounters_battle_scene.gd`（引擎链路）零修改全绿——`encounter_finished` 当帧时序不受影响。
- **HUD**：`test_missing_icons_render_labels_only` 锁定槽位只含 Label、文本与顺序逐字节；既有 `test_ui_hud`（29 用例）等 4 文件零修改全绿——只读快照渲染契约不变。
- **零告警刷屏**：三挂点全缺失路径零 push_warning（全量绿轮的 GUT Warnings 计数保持基线 4，无新增）；混合态各一次性汇总一条（文案经纯函数断言锁定）。
- **场景文件零改动**：`scenes/*.tscn` 与 `project.godot`、`data/**`、`src/state/**`、`src/save/**` 均未触碰（git diff 仅 src/world / src/encounters / src/ui + 新增 src/assets 与 tests）。

## 6. 限制

1. **图集仅挂首格**：ore 三态图集挂 tile (0,0)（= `_s0` 完好帧）、岩壁挂 `_tile0`；`_s1/_s2` 击数映射、glint 微光 tile animation、soil 16 变体 terrain autotile、矿井地面 source 5..8 等属 A6 §9.5-R1..R8 接线包（本包任务书明确不展开）。
2. **Boss phase2 未接线**：适配层可解析 `units/lumen_leviathan/phase1/`（候选 2）；相位切换时整体换装（含 `fx_phase_burst` 白闪帧内替换）属 A4/后续表现包。
3. **精灵画布与轨道间距**：A8 单位帧画布 128×128（Boss 256×256）大于灰盒行距 96 px，drop-in 后相邻轨视觉可能重叠——A8 §7.3 已预告舞台常量调整属 A4，本包不改 `battle.tscn` 常量。血量 Label 位置保持灰盒基线（叠加语义，任务书要求"保留血量 Label 叠加"）；A8 期望 Label 在精灵上方、构图顶部 24px 留白，生产验收时若重叠需由表现包调 Label 锚点。
4. **节点类型说明**：任务书"Sprite2D 替换"按精灵语义实现为 **AnimatedSprite2D**（SpriteBase2D 家族，承载 SpriteFrames 的 idle 循环 2fps，A8 §7.3 节奏）；测试按 AnimatedSprite2D 断言。
5. **HUD 图标范围**：任务书限定物品槽（InventoryBar）；背包面板 ItemsBox 行首图标（A7 §3.2 挂点之一）未接线。图标以注入纹理 1:1 缩放进 24×24（keep-aspect-centered）。
6. **导入前置**：res:// 资产命中优先走导入系统（编辑器/导出一致）；适配层对"未导入的新落位文件"有磁盘直读兜底，但导出包内必须已执行一次 Godot 导入（Run-Gut/Verify-Toolchain 均内置 `--import` 前置）。
7. **测试基建瞬态（范围外）**：§4 所记跨运行存档根碰撞为既有缺陷，本包未修（不属允许改动面；修复建议：隔离根加随机后缀，留作下一步建议）。
8. **A5/A8 战斗替身双合同并存**：战斗单位探测按 A8 优先（必读清单指定帧命名约定）；A5 落位静默不命中（§3.4）。

## 7. 下一步

1. **美术生产批次 1（P0）按 §3 对照表落位**：ENV-01/04..06 + 六单位 8 帧 + 六枚物品图标 → 三挂点全量命中后逐源/逐单位/逐物品验收（混合态告警清零）。
2. R1..R10 接线包派发（采空坑底、矿态切换、矿井地面/autotile、OreHighlight、cell_break 特效、建筑精灵、WorldBorder、Boss 刻印、Parallax、CanvasModulate）——均可用 `AssetAdapter.texture_at()/sprite_frames()` 或扩展候选直接消费本层。
3. A4 战斗表现包：Boss phase2 换装、舞台常量（128/256 精灵行距）、横幅/底图/图标挂点接入。
4. A7 集成包：主题 StyleBoxTexture 迁移、物品槽框/背包网格/对话框部件接入。
5. 测试基建（建议项，范围外）：隔离存档根命名加进程随机段，消除跨运行 msec 碰撞瞬态。
