# W000 Handoff

## Status

Window active. Governance, locked toolchain, Godot project, App host scene, and the test runner are verified. State/save and the Walking Skeleton remain.

## Resume protocol

1. Read `AGENTS.md`, `ops/GOAL.md`, and `ops/state.json`.
2. Confirm `last_verified_commit` is an ancestor of the current commit and that later commits contain only recorded checkpoint metadata or submitted work.
3. Run the commands recorded in the latest packet evidence.
4. Resume only from `resume_from` and `next_packet_ids`.

## Checkpoints

- `W000-P00` — repository/toolchain governance baseline: `29d9d49`.
- `W000-P01` — Godot project and test harness: `cdd58b4bb1939af172024b36244d327fa8a4a775`; specification and quality reviews passed; coordinator fresh verification passed.

Resume from `W000-P02`: implement atomic `GameState`/`StatePatch` and Save v1 round-trip/recovery test-first.

